# Physical AI Robot 기사 자동 수집 시스템 구현 가이드

## 📋 구현 체크리스트

### Phase 1: 기본 수집 시스템

#### 1.1 패키지 설치
```bash
npm install rss-parser cheerio node-fetch date-fns string-similarity
npm install --save-dev @types/rss-parser
```

#### 1.2 MongoDB 스키마 정의
- [ ] `collected_news` 컬렉션 생성
- [ ] 인덱스 생성 (sourceUrl, publishedAt, isActive 등)
- [ ] TypeScript 인터페이스 정의

#### 1.3 RSS 피드 수집 로직
- [ ] RSS 파서 구현
- [ ] 여러 RSS 피드 소스 관리
- [ ] 에러 핸들링

#### 1.4 중복 체크 로직
- [ ] URL 기반 중복 체크
- [ ] 제목 유사도 검사
- [ ] 발행일 기준 필터링

#### 1.5 관련성 점수 계산
- [ ] 키워드 매칭 알고리즘
- [ ] 점수 계산 로직
- [ ] 임계값 설정 (예: 50점 이상만 저장)

---

### Phase 2: 이미지 처리

#### 2.1 이미지 다운로드
- [ ] 이미지 URL 추출
- [ ] 이미지 다운로드 로직
- [ ] 이미지 타입 검증

#### 2.2 이미지 최적화
- [ ] Sharp를 사용한 리사이즈
- [ ] 이미지 포맷 변환 (WebP)
- [ ] 압축 최적화

#### 2.3 이미지 저장
- [ ] Vercel Blob Storage 업로드
- [ ] CDN URL 저장
- [ ] 실패 시 원본 URL 보관

---

### Phase 3: 게시판 UI

#### 3.1 목록 페이지
- [ ] 기사 카드 컴포넌트
- [ ] 페이지네이션
- [ ] 필터링 UI
- [ ] 정렬 기능

#### 3.2 상세 페이지
- [ ] 기사 전체 내용 표시
- [ ] 원본 링크 버튼
- [ ] 관련 기사 추천
- [ ] 조회수 증가

#### 3.3 검색 기능
- [ ] 키워드 검색
- [ ] 카테고리 필터
- [ ] 날짜 범위 필터

---

### Phase 4: 자동화

#### 4.1 Vercel Cron 설정
- [ ] `vercel.json`에 Cron 작업 추가
- [ ] 수집 API 엔드포인트 생성
- [ ] 스케줄 테스트

#### 4.2 모니터링
- [ ] 수집 로그 저장
- [ ] 실패 알림 설정
- [ ] 수집 상태 대시보드

---

### Phase 5: 관리 기능

#### 5.1 관리자 페이지 통합
- [ ] 수집된 기사 목록 표시
- [ ] 기사 활성화/비활성화
- [ ] 기사 삭제
- [ ] 수동 수집 버튼

#### 5.2 수집 설정
- [ ] RSS 피드 URL 관리
- [ ] 키워드 관리
- [ ] 수집 주기 설정

---

## 🔧 기술 스택 상세

### 백엔드
- **Next.js API Routes:** 수집 및 조회 API
- **MongoDB:** 기사 데이터 저장
- **Vercel Cron:** 자동 수집 스케줄링
- **Sharp:** 이미지 처리
- **Vercel Blob:** 이미지 저장

### 프론트엔드
- **Next.js Pages/App Router:** 게시판 페이지
- **React:** UI 컴포넌트
- **Tailwind CSS:** 스타일링
- **TypeScript:** 타입 안정성

### 외부 서비스
- **RSS 피드:** 뉴스 소스
- **Google News RSS:** 검색 기반 수집
- **네이버 검색 API:** (선택) 추가 수집

---

## 📝 API 명세서

### 수집 API

#### `POST /api/news/collect`
**설명:** 수동으로 기사 수집 실행

**인증:** 관리자만 접근 가능

**요청:**
```json
{
  "sources": ["rss1", "rss2"],  // 선택: 특정 소스만 수집
  "force": false                // 선택: 중복 체크 무시
}
```

**응답:**
```json
{
  "success": true,
  "data": {
    "collected": 5,
    "duplicates": 2,
    "failed": 0,
    "duration": 1234
  }
}
```

#### `GET /api/news/collect/status`
**설명:** 최근 수집 상태 조회

**응답:**
```json
{
  "success": true,
  "data": {
    "lastRun": "2025-01-15T10:00:00Z",
    "nextRun": "2025-01-15T16:00:00Z",
    "totalCollected": 150,
    "last24Hours": 12
  }
}
```

### 조회 API

#### `GET /api/collected-news`
**설명:** 수집된 기사 목록 조회

**쿼리 파라미터:**
- `page`: 페이지 번호 (기본값: 1)
- `limit`: 페이지당 항목 수 (기본값: 12)
- `category`: 카테고리 필터
- `keyword`: 검색 키워드
- `sort`: 정렬 방식 (`newest`, `relevance`)
- `from`: 시작 날짜 (YYYY-MM-DD)
- `to`: 종료 날짜 (YYYY-MM-DD)

**응답:**
```json
{
  "success": true,
  "data": {
    "articles": [...],
    "pagination": {
      "page": 1,
      "limit": 12,
      "total": 150,
      "totalPages": 13
    }
  }
}
```

#### `GET /api/collected-news/[id]`
**설명:** 특정 기사 상세 조회

**응답:**
```json
{
  "success": true,
  "data": {
    "_id": "...",
    "title": "...",
    "content": "...",
    "source": "...",
    "sourceUrl": "...",
    "imageUrl": "...",
    "publishedAt": "...",
    "viewCount": 123
  }
}
```

---

## 🗄️ 데이터베이스 스키마

### collected_news 컬렉션

```typescript
{
  _id: ObjectId,
  title: string,              // 기사 제목
  content: string,             // 기사 본문 (HTML 또는 텍스트)
  excerpt: string,            // 요약 (200자 이내)
  source: string,              // 출처 (예: "천안아산신문")
  sourceUrl: string,          // 원본 URL (unique)
  imageUrl?: string,          // 썸네일 이미지 URL
  publishedAt: Date,          // 발행일
  collectedAt: Date,          // 수집일
  keywords: string[],          // 관련 키워드
  category: 'education' | 'technology' | 'competition' | 'general',
  relevanceScore: number,     // 관련성 점수 (0-100)
  isActive: boolean,          // 게시 여부
  viewCount: number,          // 조회수
  createdAt: Date,
  updatedAt: Date
}
```

### 인덱스

```javascript
// 1. sourceUrl (unique) - 중복 체크용
db.collected_news.createIndex({ sourceUrl: 1 }, { unique: true });

// 2. publishedAt (descending) - 최신순 정렬
db.collected_news.createIndex({ publishedAt: -1 });

// 3. isActive + publishedAt - 활성 기사 최신순
db.collected_news.createIndex({ isActive: 1, publishedAt: -1 });

// 4. collectedAt - 수집일 기준 조회
db.collected_news.createIndex({ collectedAt: -1 });

// 5. keywords - 키워드 검색
db.collected_news.createIndex({ keywords: 1 });

// 6. category + publishedAt - 카테고리별 최신순
db.collected_news.createIndex({ category: 1, publishedAt: -1 });

// 7. relevanceScore - 관련성 순 정렬
db.collected_news.createIndex({ relevanceScore: -1 });
```

---

## 🔄 워크플로우 상세

### 수집 프로세스 플로우차트

```
시작
  ↓
RSS 피드 목록 로드
  ↓
각 RSS 피드 순회
  ↓
RSS 피드 파싱
  ↓
기사 항목 추출
  ↓
키워드 필터링
  ↓
중복 체크 (sourceUrl)
  ↓
관련성 점수 계산
  ↓
점수 >= 임계값?
  ├─ 아니오 → 다음 기사
  └─ 예 → 이미지 다운로드
         ↓
      이미지 최적화
         ↓
      Vercel Blob 업로드
         ↓
      MongoDB 저장
         ↓
      다음 기사
  ↓
모든 피드 완료
  ↓
수집 결과 반환
  ↓
종료
```

### 관련성 점수 계산 알고리즘

```typescript
function calculateRelevanceScore(article: Article, keywords: string[]): number {
  let score = 0;
  
  // 제목에 키워드 포함 (30점)
  const titleLower = article.title.toLowerCase();
  keywords.forEach(keyword => {
    if (titleLower.includes(keyword.toLowerCase())) {
      score += 30;
    }
  });
  
  // 본문에 키워드 포함 (20점)
  const contentLower = article.content.toLowerCase();
  keywords.forEach(keyword => {
    const matches = (contentLower.match(new RegExp(keyword.toLowerCase(), 'g')) || []).length;
    score += Math.min(matches * 5, 20);
  });
  
  // 출처 신뢰도 (20점)
  const trustedSources = ['천안아산신문', '충청일보', '로봇신문'];
  if (trustedSources.includes(article.source)) {
    score += 20;
  }
  
  // 최근 기사 가산점 (10점)
  const daysSincePublished = (Date.now() - article.publishedAt.getTime()) / (1000 * 60 * 60 * 24);
  if (daysSincePublished <= 7) {
    score += 10;
  }
  
  // 최대 100점
  return Math.min(score, 100);
}
```

---

## 🎨 UI/UX 설계

### 게시판 목록 페이지

**레이아웃:**
```
┌─────────────────────────────────────────┐
│  Physical AI Robot 관련 뉴스            │
│  [필터] [검색] [정렬]                   │
├─────────────────────────────────────────┤
│  ┌─────┐  ┌─────┐  ┌─────┐            │
│  │기사1│  │기사2│  │기사3│            │
│  └─────┘  └─────┘  └─────┘            │
│  ┌─────┐  ┌─────┐  ┌─────┐            │
│  │기사4│  │기사5│  │기사6│            │
│  └─────┘  └─────┘  └─────┘            │
├─────────────────────────────────────────┤
│  [이전] 1 2 3 4 5 [다음]                │
└─────────────────────────────────────────┘
```

**기사 카드 구성:**
- 이미지 (16:9 비율)
- 제목 (2줄 제한)
- 출처 및 날짜
- 요약 (3줄 제한)
- "자세히 보기" 버튼

### 필터 옵션

**카테고리:**
- 전체
- 교육
- 기술
- 대회
- 일반

**정렬:**
- 최신순
- 관련성순
- 조회수순

**날짜 범위:**
- 전체
- 오늘
- 이번 주
- 이번 달
- 최근 3개월

---

## 🔐 보안 및 권한

### API 권한

**공개 API (인증 불필요):**
- `GET /api/collected-news`
- `GET /api/collected-news/[id]`

**관리자 전용 (인증 필요):**
- `POST /api/news/collect`
- `GET /api/news/collect/status`
- `PUT /api/collected-news/[id]`
- `DELETE /api/collected-news/[id]`

### Rate Limiting

**수집 API:**
- 관리자당 1시간에 1회 제한
- Cron 작업은 제한 없음

**조회 API:**
- IP당 1분에 60회 제한

---

## 📊 모니터링 및 로깅

### 수집 로그

```typescript
interface CollectionLog {
  _id: string;
  startedAt: Date;
  completedAt?: Date;
  status: 'running' | 'completed' | 'failed';
  sources: string[];
  collected: number;
  duplicates: number;
  failed: number;
  errors?: string[];
}
```

### 알림 설정

- 수집 실패 시 관리자 이메일 알림
- 일일 수집 요약 리포트
- 이상 패턴 감지 (수집량 급감 등)

---

## 🧪 테스트 계획

### 단위 테스트
- RSS 파싱 로직
- 중복 체크 로직
- 관련성 점수 계산
- 이미지 처리 로직

### 통합 테스트
- 전체 수집 프로세스
- API 엔드포인트
- 데이터베이스 작업

### E2E 테스트
- 사용자 시나리오
- 관리자 시나리오
- 에러 처리 시나리오

---

## 📅 타임라인

### Week 1: 기본 수집 시스템
- Day 1-2: 패키지 설치 및 환경 설정
- Day 3-4: RSS 파싱 구현
- Day 5: 중복 체크 및 관련성 점수 계산

### Week 2: 이미지 처리 및 저장
- Day 1-2: 이미지 다운로드 및 최적화
- Day 3: Vercel Blob 통합
- Day 4-5: 에러 처리 및 테스트

### Week 3: 게시판 UI
- Day 1-3: 목록 페이지 구현
- Day 4-5: 상세 페이지 및 필터링

### Week 4: 자동화 및 최적화
- Day 1-2: Vercel Cron 설정
- Day 3: 관리 기능 통합
- Day 4-5: 테스트 및 버그 수정

---

## 🚀 배포 체크리스트

### 환경 변수
- [ ] `MONGODB_URI` 설정 확인
- [ ] `BLOB_READ_WRITE_TOKEN` 설정 확인
- [ ] RSS 피드 URL 목록 확인

### Vercel 설정
- [ ] Cron 작업 등록
- [ ] 환경 변수 설정
- [ ] 빌드 설정 확인

### 데이터베이스
- [ ] 컬렉션 생성
- [ ] 인덱스 생성
- [ ] 초기 데이터 확인

---

**작성일:** 2025-01-XX  
**버전:** 1.0

