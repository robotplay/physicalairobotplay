# 포트원 macOS 호환성 문제 해결

## 🔍 문제: "HTML5_INICIS 에 대해 지원하지 않는 기능입니다"

이 에러는 macOS에서 포트원의 HTML5_INICIS 결제 방식을 사용하려고 할 때 발생합니다.

---

## ✅ 해결 방법

### 원인
- `payMethod: 'CARD'`를 지정하면 포트원이 HTML5_INICIS를 사용하려고 시도
- macOS에서는 HTML5_INICIS가 지원되지 않음

### 해결책
**`payMethod` 파라미터를 제거**하여 포트원이 Channel 설정에 따라 자동으로 지원되는 결제 수단을 선택하도록 합니다.

---

## 📋 포트원 대시보드에서 Channel 확인

### 1단계: Channel 설정 확인

1. **포트원 대시보드 접속**
   - https://admin.portone.io 접속
   - 로그인

2. **Store 선택**
   - Store ID: `store-c7408361-d836-4e72-b76c-e328a4e3c119`

3. **Channel 확인**
   - "Channels" 또는 "결제 채널" 메뉴 클릭
   - Channel Key: `channel-key-8d151049-3196-4ed2-b59f-c4c5124c6971`

4. **Channel 설정 확인**
   - Channel의 결제 수단 설정 확인
   - macOS 호환 결제 수단이 활성화되어 있는지 확인

### 2단계: Channel 재생성 (필요한 경우)

macOS 호환성을 위해 Channel을 다시 생성할 수 있습니다:

1. **기존 Channel 삭제** (선택사항)
2. **새 Channel 생성**
   - 결제 수단: "신용카드" 또는 "CARD"
   - **"일반 결제" 또는 "표준 결제" 선택** (HTML5_INICIS가 아닌 방식)
3. **Channel Key 복사**

---

## 🔧 코드 수정 완료

이미 다음 수정이 완료되었습니다:

1. ✅ `payMethod` 파라미터 제거
2. ✅ 포트원이 Channel 설정에 따라 자동으로 결제 수단 선택
3. ✅ macOS 호환성 개선

---

## 🧪 테스트 방법

1. **신청 페이지 접속**
   ```
   http://localhost:3000/program/airplane
   ```

2. **신청서 작성 및 제출**

3. **결제 버튼 클릭**

4. **결제 창 확인**
   - macOS에서 지원되는 결제 방식으로 결제 창이 열려야 함
   - HTML5_INICIS 에러가 발생하지 않아야 함

---

## ⚠️ 참고사항

### macOS에서 지원되는 결제 방식
- 일반 카드 결제 (표준 방식)
- 모바일 결제
- 간편 결제

### macOS에서 지원되지 않는 결제 방식
- HTML5_INICIS (일부 환경에서만 지원)

### 해결 방법
- `payMethod`를 지정하지 않으면 포트원이 자동으로 지원되는 결제 방식을 선택
- Channel 설정에서 macOS 호환 결제 수단을 활성화

---

## 📞 문제가 계속되면

1. 포트원 대시보드에서 Channel 설정 확인
2. Channel을 재생성하여 macOS 호환 결제 수단 활성화
3. 포트원 고객센터에 문의

**수정이 완료되었습니다. 다시 테스트해보세요!**







