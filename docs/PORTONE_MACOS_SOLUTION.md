# 포트원 macOS HTML5_INICIS 에러 해결

## 🔍 문제

**에러 메시지:**
```
결제 창 호출에 실패하였습니다. 요청을 파싱하는 과정에서 에러가 발생했습니다. 
HTML5_INICIS 에 대해 지원하지 않는 기능입니다.
```

**원인:**
- macOS에서는 HTML5_INICIS 결제 방식이 지원되지 않음
- `payMethod: 'CARD'`를 지정하면 포트원이 HTML5_INICIS를 사용하려고 시도

---

## ✅ 해결 방법

### 1. payMethod 제거 (완료)

코드에서 `payMethod` 파라미터를 제거하여 포트원이 Channel 설정에 따라 자동으로 지원되는 결제 수단을 선택하도록 했습니다.

**수정된 코드:**
```typescript
const response = await portone.requestPayment({
    storeId: storeId,
    channelKey: channelKey,
    paymentId: paymentId,
    orderName: orderName,
    totalAmount: amount,
    currency: 'KRW',
    // payMethod 제거 - 포트원이 Channel 설정에 따라 자동 선택
    customer: {
        fullName: customerName,
        email: customerEmail,
        phoneNumber: customerPhone,
    },
    // ...
});
```

### 2. 포트원 대시보드에서 Channel 확인

포트원 대시보드에서 Channel이 macOS 호환 결제 수단을 지원하는지 확인하세요:

1. **포트원 대시보드 접속**
   - https://admin.portone.io
   
2. **Store 선택**
   - Store ID: `store-c7408361-d836-4e72-b76c-e328a4e3c119`

3. **Channel 확인**
   - Channel Key: `channel-key-8d151049-3196-4ed2-b59f-c4c5124c6971`
   - Channel의 결제 수단 설정 확인
   - **"일반 결제" 또는 "표준 결제" 방식이 활성화되어 있는지 확인**

### 3. Channel 재생성 (필요한 경우)

만약 문제가 계속되면 Channel을 재생성하세요:

1. **기존 Channel 삭제** (선택사항)
2. **새 Channel 생성**
   - 결제 수단: "신용카드"
   - **결제 방식: "일반 결제" 또는 "표준 결제" 선택**
   - HTML5_INICIS가 아닌 일반 결제 방식 선택
3. **새 Channel Key 복사**
4. **`.env.local` 파일 업데이트**

---

## 🧪 테스트

1. **신청 페이지 접속**
   ```
   http://localhost:3000/program/airplane
   ```

2. **신청서 작성 및 제출**

3. **결제 버튼 클릭**

4. **결제 창 확인**
   - macOS에서 지원되는 결제 방식으로 결제 창이 열려야 함
   - HTML5_INICIS 에러가 발생하지 않아야 함

---

## ⚠️ 참고사항

### macOS에서 지원되는 결제 방식
- ✅ 일반 카드 결제 (표준 방식)
- ✅ 모바일 결제
- ✅ 간편 결제

### macOS에서 지원되지 않는 결제 방식
- ❌ HTML5_INICIS (일부 환경에서만 지원)

### 해결 방법
- `payMethod`를 지정하지 않으면 포트원이 Channel 설정에 따라 자동으로 지원되는 결제 방식을 선택
- Channel 설정에서 macOS 호환 결제 수단을 활성화

---

## 📞 문제가 계속되면

1. 포트원 대시보드에서 Channel 설정 확인
2. Channel을 재생성하여 macOS 호환 결제 수단 활성화
3. 포트원 고객센터에 문의

**수정이 완료되었습니다. 다시 테스트해보세요!**




